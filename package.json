{
  "name": "beyond-sundays",
  "private": true,
  "description": "Dependencies for the Netlify scheduled reminder function — the site itself is plain HTML/CSS/JS with no build step.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "twilio": "^5.3.0"
  }
}
